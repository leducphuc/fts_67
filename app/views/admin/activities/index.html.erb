<% @activities.each do |activity| %>
  <% if activity.deleted? %>
    <%= activity.description %>
  <% else %>
    <%= link_to activity.description, [:admin, activity.activable] %>
  <% end %>
<% end %>
